name: First workflow
on: workflow_dispatch
jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - 
                name: Print hello
                run: echo "Hello World"
            - 
                name : Print Goodbye
                run: echo "good bye"
